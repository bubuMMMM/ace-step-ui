# The studio's own architect agent uses anthropic/claude-sonnet-5 through the
# Vercel AI Gateway. Without a credential the builder and the project export
# still work — only the AI assist panel goes dark.
#
# Get a key at https://vercel.com/dashboard/ai-gateway, or link a Vercel project
# and let VERCEL_OIDC_TOKEN supply it automatically.
AI_GATEWAY_API_KEY=
